# MPT Platform v0.1 安全与隐私

## 必须遵守

- 密码哈希存储；
- API Key 仅从环境变量读取；
- `.env` 不提交；
- 上传文件限制类型和大小；
- 文件名清洗并使用随机存储名；
- 不执行用户上传文件；
- 日志不记录完整简历；
- 错误响应不暴露服务器路径与堆栈；
- 删除候选人二次确认；
- 未登录不能访问业务 API。

## 允许文件

建议：

```text
.pdf
.docx
.txt
```

拒绝可执行文件、脚本、宏文档和双扩展名伪装文件。

## AI 数据最小化

只发送解析与报告所需文本，去除电话、邮箱和无关敏感信息。
